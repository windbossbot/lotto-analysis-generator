# SUPPORT_MEANS_POLICY_CURRENT

Updated: 2026-04-03

## Purpose

Keep support means recommendations in `lotto` useful, visible, and light.

## Bracket Tag Rule

When support means are actually used in user-facing updates or closeout, mark them briefly:

1. `[judge_request.py 스크립트]`
2. `[myclaw-admin-mode 스킬]`
3. `[worker 에이전트]`
4. `[daily-review 자동화]`
5. `[HELPER_SESSION_PACKET_TEMPLATE 템플릿]`
6. `[PROJECT_TRUTH_CURRENT.md 문서]`

When only recommending them, prefer:

1. `[truth_pack_builder.py 스크립트 추천]`
2. `[project-kernel-extractor 스킬 추천]`
3. `[bounded helper agent 에이전트 추천]`
4. `[later recurring task 자동화 추천]`
5. `[HELPER_SESSION_PACKET_TEMPLATE 템플릿 추천]`

## Early Recommendation Window

1. treat the first roughly 5 to 10 user questions in this project as a proactive support-recommendation window
2. during that window, recommend helpful scripts, skills, agents, templates, or automations more actively when they are genuinely useful
3. after that window, switch to lighter on-demand recommendation unless the payoff is obvious

## Shared Core Rule

1. if a helper looks reusable across projects, recommend it softly toward the shared core at `C:\Users\KGWPC\workspace\myclaw`
2. do not block current project work on shared-core extraction unless the helper pays off immediately
3. actual shared-core edits still belong to engine admin mode
